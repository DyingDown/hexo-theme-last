extends includes/layout.pug

block content
  include includes/recent-posts.pug
  if page.type === links
    
    h1!= Friends
    h1!= Friends
    h1!= Friends
  include includes/pagination.pug
  //- #paginator!= paginator()
